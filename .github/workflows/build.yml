name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Linux x64
            os: ubuntu-22.04
            platform: linux
            tauri_args: --ci --no-sign
          - label: Windows x64
            os: windows-latest
            platform: windows
            tauri_args: --ci --no-sign
          - label: macOS Intel
            os: macos-latest
            platform: macos
            tauri_args: --ci --no-sign --bundles app --target x86_64-apple-darwin
          - label: macOS Apple Silicon
            os: macos-latest
            platform: macos
            tauri_args: --ci --no-sign --bundles app --target aarch64-apple-darwin

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.label }})

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.93.0

      - name: Install macOS Rust targets
        if: runner.os == 'macOS'
        run: rustup target add aarch64-apple-darwin x86_64-apple-darwin

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/app/src-tauri -> target

      - name: Cache Tauri Linux bundler tools
        if: matrix.platform == 'linux'
        uses: actions/cache@v4
        with:
          path: ~/.cache/tauri
          key: tauri-linux-tools-v1

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install JS dependencies
        run: bun install

      - name: Prefetch Tauri Linux bundler tools
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "${HOME}/.cache/tauri"

          curl --fail --location --retry 5 --retry-all-errors --retry-delay 5 \
            --output "${HOME}/.cache/tauri/AppRun-x86_64" \
            "https://github.com/tauri-apps/binary-releases/releases/download/apprun-old/AppRun-x86_64"
          chmod +x "${HOME}/.cache/tauri/AppRun-x86_64"

          curl --fail --location --retry 5 --retry-all-errors --retry-delay 5 \
            --output "${HOME}/.cache/tauri/linuxdeploy-x86_64.AppImage" \
            "https://github.com/tauri-apps/binary-releases/releases/download/linuxdeploy/linuxdeploy-x86_64.AppImage"
          chmod +x "${HOME}/.cache/tauri/linuxdeploy-x86_64.AppImage"

          curl --fail --location --retry 5 --retry-all-errors --retry-delay 5 \
            --output "${HOME}/.cache/tauri/linuxdeploy-plugin-gtk.sh" \
            "https://raw.githubusercontent.com/tauri-apps/linuxdeploy-plugin-gtk/master/linuxdeploy-plugin-gtk.sh"
          chmod +x "${HOME}/.cache/tauri/linuxdeploy-plugin-gtk.sh"

      - name: Validate release contract
        run: bun run release:check

      - name: Build
        run: bun run --cwd apps/app tauri build ${{ matrix.tauri_args }}
